# Database (Drizzle ORM with PostgreSQL)
DATABASE_URL="postgresql://atlas:atlas_password@localhost:5432/atlas_db"

# For Docker (use service name instead of localhost)
# DATABASE_URL="postgresql://atlas:atlas_password@postgres:5432/atlas_db"

# Application
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Process Dashboard Link (shown on projects page)
# Leave empty to hide the link
NEXT_PUBLIC_PROCESS_DASHBOARD_URL=""

# User Information (JSON format)
# Used for user initialization and authentication
USERINFO='{"email":"user@example.com","name":"User Name"}'

# Optional: Analytics
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=""

# Logging
LOG_LEVEL=trace
