# Data Flow Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                        Browser (Client)                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   page.tsx   │  │ DataTable    │  │   columns    │      │
│  │              │  │              │  │              │      │
│  │ useQuery()   │→ │ data props   │→ │ editable     │      │
│  │ useSSE()     │  │              │  │ cells        │      │
│  └──────────────┘  └──────────────┘  └──────┬───────┘      │
│         ↓                                    ↓               │
│  ┌──────────────────────────────────────────────────┐       │
│  │           React Query Cache                      │       │
│  │  { tasks: [...], dbTasks: [...] }                │       │
│  └───────┬──────────────────────────────────┬───────┘       │
│          ↓ Mutations                        ↓ SSE Updates   │
└──────────┼──────────────────────────────────┼───────────────┘
           │                                   │
           │ HTTP PATCH                        │ EventSource
           │                                   │
┌──────────┼───────────────────────────────────┼───────────────┐
│  Next.js │ Server                            │               │
├──────────┼───────────────────────────────────┼───────────────┤
│          ↓                                   ↓               │
│  ┌────────────────┐              ┌────────────────┐         │
│  │ /api/tasks/[id]│              │ /api/tasks/    │         │
│  │  PATCH         │              │ [wid]/events   │         │
│  │                │              │  SSE Stream    │         │
│  └────────┬───────┘              └────────┬───────┘         │
│           │                               │                 │
│           ↓                               ↓                 │
│  ┌────────────────┐              ┌────────────────┐         │
│  │ Drizzle ORM    │              │ SSE Broadcast  │         │
│  │ JSONB Merge    │→ Update DB → │ (in-memory)    │         │
│  └────────┬───────┘              └────────────────┘         │
│           │                                                  │
└───────────┼──────────────────────────────────────────────────┘
            ↓
┌───────────────────────────────────────────────────────────────┐
│                      PostgreSQL Database                       │
├───────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐         │
│  │ workspaces  │  │ field_      │  │ tasks        │         │
│  │             │  │ configs     │  │              │         │
│  │ id (UUID)   │  │             │  │ id (UUID)    │         │
│  │ numeric_id  │  │ key         │  │ displayId    │         │
│  │ name        │  │ type        │  │ data (JSONB) │         │
│  │ slug        │  │ options     │  │ version      │         │
│  └─────────────┘  └─────────────┘  └──────────────┘         │
└───────────────────────────────────────────────────────────────┘
```

## Optimistic Update Flow

```
User Action: Edit cell "status" from "todo" to "done"
     │
     ↓
┌────────────────────────────────────────────────┐
│ 1. onMutate (Optimistic)                       │
│    - Cancel in-flight queries                  │
│    - Snapshot current cache                    │
│    - Update cache immediately                  │
│    → UI shows "done" instantly ⚡              │
└────────┬───────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────┐
│ 2. Network Request (Background)                │
│    PATCH /api/tasks/abc-123                    │
│    Body: { patch: { status: "done" } }         │
└────────┬───────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────┐
│ 3. Database Update                             │
│    UPDATE tasks                                │
│    SET data = data || '{"status":"done"}'      │
│    WHERE id = 'abc-123'                        │
└────────┬───────────────────────────────────────┘
         │
    ┌────┴────┐
    │         │
    ↓         ↓
SUCCESS   ERROR
    │         │
    ↓         ↓
┌──────┐  ┌──────────────────┐
│ 4a.  │  │ 4b. onError      │
│ Show │  │  - Rollback cache│
│toast │  │  - Show error    │
│"Saved│  │  toast           │
│  ✓"  │  │  ❌              │
└──────┘  └──────────────────┘
    │
    ↓
┌────────────────────────────────────────────────┐
│ 5. onSettled                                   │
│    - Invalidate queries                        │
│    - Refetch from server (truth)               │
└────────────────────────────────────────────────┘
```

## SSE Live Sync Flow

```
Tab A: User edits task
     │
     ↓
┌──────────────────────────────┐
│ 1. Optimistic Update         │
│    (Tab A cache updated)     │
└────────┬─────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│ 2. PATCH /api/tasks/[id]     │
│    (Update database)         │
└────────┬─────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│ 3. broadcastTaskUpdate()     │
│    (SSE broadcast to all)    │
└────────┬─────────────────────┘
         │
    ┌────┴────────────┐
    │                 │
    ↓                 ↓
┌────────┐      ┌──────────┐
│ Tab A  │      │  Tab B   │
│  ✓     │      │  📡      │
│Already │      │Receives  │
│updated │      │SSE event │
└────────┘      └────┬─────┘
                     │
                     ↓
              ┌─────────────────────┐
              │ 4. Update Tab B     │
              │    - Parse event    │
              │    - Update cache   │
              │    → UI re-renders  │
              │    ✨              │
              └─────────────────────┘
```

## Type Conversion Flow

```
Database (PostgreSQL)              UI (React)
┌────────────────────┐            ┌──────────────────┐
│ Task (DB Schema)   │            │ Task (UI Type)   │
├────────────────────┤            ├──────────────────┤
│ id: UUID           │            │ id: displayId    │
│ displayId: string  │◄───────────│   "TSK-001-0001" │
│ workspaceId: UUID  │            │ owner: string    │
│ sequenceNumber: int│            │ title: string    │
│ data: JSONB {      │  Mapper    │ status: Status   │
│   owner,           │◄────────►  │ priority: Prio   │
│   title,           │            │ currentHrs: num  │
│   status,          │            │ workedHrs: num   │
│   priority,        │            │ ...all flat      │
│   ...              │            │                  │
│ }                  │            │                  │
│ version: int       │            │                  │
│ createdAt: Date    │            │ createdAt: Date  │
│ updatedAt: Date    │            │ updatedAt: Date  │
└────────────────────┘            └──────────────────┘
         ↕                                ↕
  dbTaskToUiTask()              uiTaskToDbData()
```

## Request/Response Examples

### 1. GET Tasks (Paginated)
```http
GET /api/tasks?workspaceId=abc-123&page=0
```

Response:
```json
{
  "tasks": [
    {
      "id": "def-456",
      "displayId": "TSK-001-0001",
      "workspaceId": "abc-123",
      "sequenceNumber": 1,
      "data": {
        "title": "Setup CI/CD",
        "status": "in-progress",
        "priority": "high"
      },
      "version": 1,
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-02T00:00:00Z"
    }
  ],
  "page": 0,
  "perPage": 50,
  "hasMore": false
}
```

### 2. PATCH Task (Update)
```http
PATCH /api/tasks/def-456
Content-Type: application/json

{
  "patch": {
    "status": "done",
    "completedAt": "2024-01-03T00:00:00Z"
  }
}
```

Response:
```json
{
  "id": "def-456",
  "displayId": "TSK-001-0001",
  "data": {
    "title": "Setup CI/CD",
    "status": "done",
    "priority": "high",
    "completedAt": "2024-01-03T00:00:00Z"
  },
  "version": 2
}
```

### 3. SSE Events
```http
GET /api/tasks/abc-123/events
Accept: text/event-stream
```

Stream:
```
data: {"type":"connected","clientId":"abc-123-1234567890"}

data: {"type":"initial_state","tasks":[...]}

: heartbeat

data: {"type":"task_update","task":{...}}

: heartbeat
```

## Performance Characteristics

### Latency Comparison

```
Traditional Flow:
User Click → Network (200ms) → Server (50ms) → UI Update (50ms) = 300ms

Optimistic Flow:
User Click → UI Update (0ms) → Network (200ms) → Server (50ms) = 0ms perceived

Real-Time Comparison:
HTTP Polling: Check every 3s → Avg latency 1.5s
SSE Stream: Push updates → Avg latency <100ms
```

### Network Efficiency

```
Polling (10 clients, 1 update/min):
10 clients × 20 polls/min × 5kb/poll = 1000kb/min

SSE (10 clients, 1 update/min):
10 connections × 1 update/min × 1kb/update = 10kb/min
+ 10 heartbeats/min × 0.1kb = 1kb/min
= 11kb/min (99% reduction!)
```

## Error Handling States

```
┌──────────────┐
│ User Action  │
└──────┬───────┘
       │
       ↓
┌──────────────────┐
│ Optimistic UI    │ ← State 1: Success (instant)
│ ✅ Updated       │
└──────┬───────────┘
       │
       ↓
   Network Call
       │
   ┌───┴───┐
   │       │
   ↓       ↓
Success  Failure
   │       │
   │       ↓
   │   ┌─────────────────┐
   │   │ Rollback UI     │ ← State 2: Error
   │   │ ❌ Reverted     │
   │   │ 🔔 Toast shown  │
   │   └─────────────────┘
   │
   ↓
┌─────────────────┐
│ Server Response │ ← State 3: Confirmed
│ ✅ Persisted    │
│ 🔔 Toast shown  │
└─────────────────┘
```
